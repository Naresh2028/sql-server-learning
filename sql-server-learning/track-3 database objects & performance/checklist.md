--track-3 database objects & performance

- [x] Table design
- [ ] constraints (PRIMARY KEY, FOREIGN KEY, UNIQUE, CHECK, DEFAULT)
- [ ] identity columns
- [ ] data types (int, bigint, smallint, tinyint, bit, varchar(x), nvarchar(x), char(x), text, money, date, decimal(p,s), bit, date )
- [ ] Indexes (clustered vs non-clustered), index strategies
- [ ] execution plans, statistics, query optimization
- [ ] basics Views, stored procedures, functions (scalar, table-valued), parameters
- [ ] best practices Triggers (INSTEAD OF, AFTER)
- [ ] temporary tables vs table variables, dynamic SQL
- [ ] Addition Isolation levels & deadlock handling


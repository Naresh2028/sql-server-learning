Database
 └── Guarantees (ACID, durability, concurrency)
     └── Tables (facts)
         └── Constraints (rules)
             └── Indexes (performance)
                 └── Views (read models)
                     └── Functions (logic)
                         └── Triggers (reactions)
                             └── Stored Procedures (APIs)
